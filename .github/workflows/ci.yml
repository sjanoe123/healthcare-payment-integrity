name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  # Backend Tests
  backend:
    name: Backend Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install pytest pytest-cov ruff

      - name: Lint with Ruff
        run: |
          cd backend && ruff check .
          cd backend && ruff format --check .
        # Linting is enforced - failures will block merge

      - name: Run tests
        run: |
          pytest tests/ -v --ignore=tests/test_real_data.py --ignore=tests/test_kirk.py
        env:
          TESTING: "true"
        # Tests are enforced - failures will block merge

  # Frontend Tests & Build
  frontend:
    name: Frontend Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit
        # TypeScript errors will block merge

      - name: Lint
        run: npm run lint
        # ESLint errors will block merge

      - name: Build
        run: npm run build
        # Build failures will block merge

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 7

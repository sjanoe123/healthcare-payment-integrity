# Anthropic Claude API Key (optional but recommended for AI explanations)
ANTHROPIC_API_KEY=your-api-key-here

# Database path (defaults to ./data/prototype.db)
DB_PATH=./data/prototype.db

# ChromaDB persistence directory (defaults to ./data/chroma)
CHROMA_PERSIST_DIR=./data/chroma
